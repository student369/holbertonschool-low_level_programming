#include "holberton.h"

/**
 * _strcmp - function to compare two strings.
 * @s1: the first string to compare
 * @s2: the second string to compare
 * Return: A comparation number.
 */
int _strcmp(char *s1, char *s2)
{
	return (s1[0] - s2[0]);
}
