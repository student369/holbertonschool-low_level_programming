#include "holberton.h"

/**
 * _islower - function to evaluate if a text
 * is in lowercase.
 * @c: the character to evaluate
 * Return: 1 is is or 0 if not
 */
int _islower(int c)
{
	return ((c >= 'a' && c <= 'z'));
}
