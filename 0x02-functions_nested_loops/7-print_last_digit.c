#include "holberton.h"

/**
 * print_last_digit - get the absolute value from the number
 * formed part of the alphabet
 * @c: The character to verify
 * Return: 1 if is positive -1 if is negative and 0 if is zero
 */
int print_last_digit(int n)
{
	return (n % 10);
}
