#include <unistd.h>

/**
 * add - funtion to add two numbers
 * @x: The first number to operate
 * @y: The second number to operate
 *
 * Return: the sum result.
 */
int add(int x, int y)
{	
	return (x + y);
}
